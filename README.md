# lazy-sketch
plugin for obsidian to generate sketches with ai

when activated, type what u want to see and use api with replicate to generate the image and paste it into current file where the cursor is
